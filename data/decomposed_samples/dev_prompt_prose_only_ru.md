Dev task instructions

Назначение файла

Этот файл моделирует задачу, которую разработчик даёт coding agent в существующем репозитории.
Он не является глобальной памятью проекта и не заменяет или .
Его задача — описать конкретное изменение, ограничения, проверки и формат результата.

Context

Проект — -инструмент для измерения токенизации Markdown-файлов.
Основной сценарий — сравнить русский, английский и project instructions.
Код должен оставаться маленьким, воспроизводимым и пригодным для публикации вместе со статьёй.
Не нужно превращать этот слой в полноценный research framework.

Goal

Добавь команду , которая считает токены в Markdown samples.
Команда должна использовать локальный tokenizer через .
Команда должна поддерживать и .
Команда должна сохранять результат в .
Команда должна быть пригодна для повторного запуска без ручной очистки файлов.

Non-goals

Не добавляй подсчёт Claude в этом слое.
Не добавляй подсчёт Gemini в этом слое.
Не добавляй расчёт стоимости в этом слое.
Не добавляй prompt caching в этом слое.
Не делай веб-интерфейс.
Не переписывай структуру проекта.
Не добавляй , если стандартного достаточно.

Input files

Скрипт должен читать Markdown-файлы из .
Strict пары имеют вид и .
пары имеют вид и .
sample может быть сгенерирован как и .
Старые samples не должны участвовать в основном запуске.

Output files

Основной должен лежать в .
Metadata должна лежать в .
должен быть перезаписываемым и детерминированным.
Metadata должна фиксировать дату запуска, версию и список encodings.

Required columns

— имя sample без языкового суффикса.
— тип sample: corpus, , или .
— тип сравнения: или .
— язык текущей строки: , или .
— формат входного файла, сейчас .
— имя tokenizer encoding.
— число Unicode characters.
— число -8 bytes.
— грубая оценка слов через whitespace split.
— число строк в Markdown-файле.
— число токенов.
— отношение compared tokens к English baseline.

Implementation constraints

Сохрани скрипт в .
Не добавляй скрытых network calls.
Не используй keys.
Не меняй generated files.
Не коммить raw text, если условия датасета запрещают redistribution.
Пиши код так, чтобы его можно было прочитать в статье.
Раздели поиск файлов, группировку пар, подсчёт токенов и запись .
Обрабатывай отсутствие как warning, а не как fatal error.

Verification

После изменений запусти команду:

Затем проверь первые строки :

Потом выведи компактную Markdown-таблицу ratio.
В таблице должны быть строки для strict и comparisons.
В таблице должны быть оба encodings: и .

Acceptance criteria

Команда запускается из корня проекта.
создаётся без ручной подготовки папки .
Markdown samples считаются по умолчанию.
samples не участвуют в основном измерении.
Strict и не смешиваются в одной метрике.
В metadata видно, какие sample pairs были найдены.
Если отсутствует, скрипт продолжает работу на practical samples.
Код не требует внешних .
Код не требует сетевого доступа.
Код не добавляет нерелевантных зависимостей.

Final response format

В финальном ответе перечисли:

изменённые файлы
созданные файлы
команды, которые были запущены
результат проверки
что не удалось проверить
оставшиеся методологические риски.

Notes

Не делай финальных выводов для статьи на этом этапе.
Layer 02 фиксирует только raw tokenizer count.
Claude, Gemini, usage и price будут отдельными слоями.
Если результат кажется неожиданным, сначала проверь входные Markdown files.
